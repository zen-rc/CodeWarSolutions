// instructions
    // Write function RemoveExclamationMarks which removes all exclamation marks from a given string.

// my solution
const removeExclaim = s => s.replace('/!/g', '')

// practice

    // 1
const removeExclaim = s => s.replace('/!/g', '' )

    // 2
const removeExclaim = s => s.replace('/!/g', '')

    // 3
const removeExclaim = s => s.replace('/!/g', '')

    // 4
const removeExclaim = s => s.replace('/!/g', '')

    // 5
const removeExclaim = s => s.replace('/!/g', '')

    // 6
const removeExclaim = s => s.replace('/!/g', '')

    // 7
const removeExclaim = s => s.replace('/!/g', '')

    // 8
const removeExclaim = s => s.replace('/!/g', '')

    // 9
const removeExclaim = s => s.replace('/!/g', '')

    // 10
const removeExclaim = s => s.replace('/!/g', '')