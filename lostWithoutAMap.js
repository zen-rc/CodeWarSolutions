//INSTRUCTIONS
//Given an array of integers, return a new array with each value doubled.


//PSUEDO CODE EXPLANATION


//MY SOLUTION
const dubInt = arr => {
    const doublearr = arr.map(i => i * 2)
    
    return doublearr
    }


//1
const dubInt = arr => {
    const doublearr = arr.map(i => i * 2)
}
//2
const dubInt = arr => {
    const doublearr = arr.map(i => i * 2)
}
//3
const dubINt = arr => {
    const doublearr = arr.map( i => i * 2)
}
//4
const dubInt = arr => {
    const doublearr = arr.map( i => i * 2)
}
//5
const dubInt = arr => {
    const doublearr = arr.map(i => i * 2)
}
//6
const dubInt = arr => {
    const doublearr = arr.map(i => i * 2)
}
//7
const dubInt = arr => {
    const doublearr = arr.map(i => i * 2)
}
//8
const dubInt = arr => {
    const doublearr = arr.map(i => i * 2)
}
//9
const dubInt = arr => {
    const doublearr = arr.map(i => i * 2)
}
//10
const dubInt = arr => arr.map(i => i * 2)
//OTHER SOLUTIONS